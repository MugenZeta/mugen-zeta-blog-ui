import react from React;
import {Collapse} from "reat-bootstrap";
import {Card} from "react-bootstrap";
import {Button} from "react-bootstrap";
import {useState} from "react";
import blogPostData from "./utils/blogData";
/*
export default function BlogPost(props){

    let postId = props.postID;
    let postTitle = props.postTitle;
    let postBody = props.postBody;
    let postAuthor = props.postAuthor;
    let postDate = props.postDate;

    return(
        <div>
            {blogPostData.map((blogData)) => (
                <div className="blog-preview" key={blogsData.postID}>

                </div>)}
        </div>
    )
}
*/
